from setuptools import find_packages, setup

setup(
    name='netbox-gcp',
    version='0.1',
    description='GCP plugin',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)

